export type AssistantIntent = 'PLACE_SEARCH' | 'PLACE_DETAILS' | 'NEARBY_PLACES' | 'RECOMMENDATION' | 'WEATHER' | 'ROUTING' | 'CITY_INFORMATION' | 'ALERTS' | 'GENERAL_CITY_QUERY';

export type AssistantRequest = { message: string; conversationId?: string; cityId?: string; placeId?: string; latitude?: number; longitude?: number; origin?: { latitude: number; longitude: number }; destination?: { latitude: number; longitude: number }; history?: Array<{ role: 'user' | 'assistant'; content: string }> };
export type AssistantResponse = { message: string; intent: AssistantIntent; conversationId?: string; cityId?: string; places: Array<{ placeId: string; name?: string; category?: string; subtype?: string | null; cityVerseScore?: number; reviewCount?: number; reason: string }>; details?: unknown; weather: unknown | null; route: unknown | null; alerts: unknown[]; alertsAvailable?: boolean; alertsReason?: string; suggestions: string[]; clarification?: string; unavailable?: boolean };
