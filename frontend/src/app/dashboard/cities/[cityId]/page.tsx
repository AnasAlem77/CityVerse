"use client";

import { useEffect, useState } from "react";
import { CityDashboard, getCityDashboard } from "@/lib/api";

export default function CityDashboardPage({ params }: { params: Promise<{ cityId: string }> }) {
  const [data, setData] = useState<CityDashboard | null>(null); const [error, setError] = useState("");
  useEffect(() => { params.then(({ cityId }) => getCityDashboard(cityId).then(setData).catch((cause) => setError(cause instanceof Error ? cause.message : "Dashboard unavailable"))); }, [params]);
  if (error) return <main className="min-h-screen bg-[var(--background)] px-6 pb-20 pt-32"><section className="mx-auto max-w-7xl"><p className="text-[var(--muted)]">{error}. Administrator access is required.</p></section></main>;
  if (!data) return <main className="min-h-screen bg-[var(--background)] px-6 pb-20 pt-32"><section className="mx-auto max-w-7xl"><p className="text-[var(--muted)]">Loading city analytics...</p></section></main>;
  return <main className="min-h-screen bg-[var(--background)] px-6 pb-20 pt-32"><section className="mx-auto max-w-7xl"><p className="text-sm font-bold uppercase tracking-[0.2em] text-[var(--primary)]">City analytics</p><h1 className="mt-3 text-4xl font-black text-[var(--foreground)]">{data.city.name}</h1><p className="mt-2 text-[var(--muted)]">{data.city.country} · {data.city.timezone}</p><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><div className="rounded-3xl border border-[var(--border)] bg-[var(--card)] p-5"><p className="text-sm text-[var(--muted)]">Total places</p><p className="mt-2 text-3xl font-black text-[var(--foreground)]">{data.totalPlaces.toLocaleString()}</p></div><div className="rounded-3xl border border-[var(--border)] bg-[var(--card)] p-5"><p className="text-sm text-[var(--muted)]">Average CityVerse Score</p><p className="mt-2 text-3xl font-black text-[var(--foreground)]">{data.scoreStats.average?.toFixed(2) ?? "Not available"}</p></div><div className="rounded-3xl border border-[var(--border)] bg-[var(--card)] p-5"><p className="text-sm text-[var(--muted)]">User rating average</p><p className="mt-2 text-3xl font-black text-[var(--foreground)]">{data.ratings.average?.toFixed(2) ?? "Not available"}</p></div><div className="rounded-3xl border border-[var(--border)] bg-[var(--card)] p-5"><p className="text-sm text-[var(--muted)]">Review count</p><p className="mt-2 text-3xl font-black text-[var(--foreground)]">{data.ratings.reviewCount.toLocaleString()}</p></div></div><div className="mt-8 grid gap-6 lg:grid-cols-2"><section className="rounded-3xl border border-[var(--border)] bg-[var(--card)] p-6"><h2 className="text-xl font-black text-[var(--foreground)]">Category distribution</h2><div className="mt-5 space-y-3">{data.categories.map((item) => <div key={item.category} className="flex items-center justify-between text-sm"><span className="capitalize text-[var(--foreground)]">{item.category.replaceAll("_", " ")}</span><span className="text-[var(--muted)]">{item.count.toLocaleString()} · {item.percentage}%</span></div>)}</div></section><section className="rounded-3xl border border-[var(--border)] bg-[var(--card)] p-6"><h2 className="text-xl font-black text-[var(--foreground)]">Data quality</h2><div className="mt-5 grid grid-cols-2 gap-3">{Object.entries(data.quality).map(([key, value]) => <div key={key} className="rounded-2xl bg-[var(--background)] p-3"><p className="text-xs capitalize text-[var(--muted)]">{key.replaceAll("_", " ")}</p><p className="mt-1 font-bold text-[var(--foreground)]">{value.percentage}%</p></div>)}</div></section></div><section className="mt-6 rounded-3xl border border-[var(--border)] bg-[var(--card)] p-6"><h2 className="text-xl font-black text-[var(--foreground)]">Highest CityVerse Scores</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{data.topPlaces.byCityVerseScore.map((item) => <div key={item.placeId} className="rounded-2xl border border-[var(--border)] p-4"><p className="font-bold text-[var(--foreground)]">{item.place.name}</p><p className="text-sm capitalize text-[var(--muted)]">{item.place.category}{item.place.subtype ? ` · ${item.place.subtype}` : ""}</p><p className="mt-2 text-sm font-semibold text-[var(--primary)]">CityVerse Score: {item.score.toFixed(1)}</p></div>)}</div></section></section></main>;
}
